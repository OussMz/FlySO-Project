apiVersion: v1
kind: Service
metadata:
  name: processing-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: processing-service
  ports:
    - protocol: TCP
      port: 6000
      targetPort: 6000
